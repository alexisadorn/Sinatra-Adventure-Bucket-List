<h1>Categories</h1>
<% Category.all.each do |category| %>
  <ul>
    <% if !category.experiences.empty? %>
      <li><a href="/categories/<%= category.id %>"><%= category.name %></a>(<%= category.experiences.length %>)</li>
    <% end %>
  </ul>
<% end %>
